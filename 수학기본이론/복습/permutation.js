let input = ['a','b','c'];
let count = 0;

function permutation(arr, s, r){
    //멈출 조건
    if( s == r){
        console.log(arr.join(''));
        return;
    }
    
    //반복시킬 조건
    

}

permutation(input, 0, 2);

console.log(count);